<?php
$title = 'Excluir um registro';
$url   = $this->url('sig-rh/fonte-seguro', ['action' => 'delete', 'id' => $fonteSeguro->getId()]);
$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>

<p>
    Deseja realmente excluir 
    "<?= $this->escapeHtml($fonteSeguro->getDescricao()); ?>"
</p>

<form action="<?= $url ?>" method="post">
    <div class="form-group">
        <input type="hidden" name="id" value="<?= (int) $fonteSeguro->getId() ?>" />
        <input type="submit" class="btn btn-danger" name="del" value="Sim" />
        <input type="submit" class="btn btn-success" name="del" value="Não" />
    </div>
</form>