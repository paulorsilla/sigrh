<h1>Importações realizadas</h1>
<div class="row">

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Referência</th>
                <th>Data da importação</th>
                <th>Usuário</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->importacoesPonto as $importacao): ?>
                <tr>
                    <td><?= $this->escapeHtml($importacao->getReferencia()); ?></td>
                    <td><?= $this->escapeHtml($importacao->getDataImportacao()->format("d-m-Y")); ?></td>
                    <td><?= $this->escapeHtml($importacao->getUsuario()->getNome()); ?></td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
    <span class="pull-right">
        <?= $this->paginationControl($this->importacoesPonto, 'Elastic', 'sig-rh/partial/paginator', ['route' => 'sig-rh/importacao-ponto']); ?>
    </span>
</div>

<div class="row">
    <a class="btn btn-primary pull-left" href="/sig-rh/importacao-ponto/save">Nova importação</a>
</div>
