<h1>Bancos cadastrados</h1>
<div class="row">

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Código</th>
                <th>Banco</th>
                <th>Opções</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->bancos as $banco): ?>
                <tr>
                    <td><?= $this->escapeHtml($banco->getId()); ?></td>
                    <td><?= $this->escapeHtml($banco->getCodigo()); ?></td>
                    <td><?= $this->escapeHtml($banco->getBanco()); ?></td>
                    <td><a class="btn btn-primary btn-sm pull-center"
                           href="<?= $this->url('sig-rh/banco', ['action' => 'save', 'id' => $banco->getId()]) ?>"
                           title="Editar registro."><span class="glyphicon glyphicon-pencil"></span></a>
                        <a class="btn btn-primary btn-sm pull-center"
                           href="<?= $this->url('sig-rh/banco', ['action' => 'delete', 'id' => $banco->getId()]) ?>"
                           title="Excluir registro."><span class="glyphicon glyphicon-remove"></span></a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
    <span class="pull-right">
        <?= $this->paginationControl($this->bancos, 'Elastic', 'sig-rh/partial/paginator', ['route' => 'sig-rh/banco']); ?>
    </span>
</div>

<div class="row">
    <a class="btn btn-primary pull-left" href="/sig-rh/banco/save">Novo
        registro</a>
</div>
